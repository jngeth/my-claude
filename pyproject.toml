[project]
name = "my-claude"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pyyaml>=6.0.3",
]

[tool.pytest.ini_options]
testpaths = ["skills"]
python_files = ["test_*.py"]
addopts = "--import-mode=importlib"
